import 'package:flutter/material.dart';

class VerablePage extends StatelessWidget {
  const VerablePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
